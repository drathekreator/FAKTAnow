{
  "name": "faktanow",
  "buildCommand": "composer install --optimize-autoloader --no-dev && npm install && npm run build && php artisan storage:link --force && mkdir -p storage/app/public/thumbnails && chmod -R 775 storage && php artisan config:cache && php artisan route:cache && php artisan view:cache",
  "startCommand": "bash startup.sh",
  "env": {
    "APP_ENV": "production",
    "APP_DEBUG": "false",
    "LOG_LEVEL": "error"
  }
}
